services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: locket_clone_db
      POSTGRES_USER: locket_clone_user
      POSTGRES_PASSWORD: locket_clone_pass
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data: